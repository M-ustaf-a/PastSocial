<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VirtuHub - Where Like-Minded People Connect & Sell Virtual Products</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #A82400;
            --primary-dark: #8A1E00;
            --secondary: #D63A0D;
            --accent: #FFEBCD;
            --dark: #1a0f0a;
            --light: #FFEBCD;
            --gray: #64748b;
            --cream: #FFF8DC;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            color: var(--dark);
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(26, 15, 10, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 1rem 0;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--light);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .logo::before {
            content: "🌐";
            font-size: 1.8rem;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-links a {
            color: var(--light);
            text-decoration: none;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: var(--secondary);
        }

        .cta-button {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 0.7rem 1.5rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(168, 36, 0, 0.3);
        }

        /* Mobile Menu */
        .mobile-menu-toggle {
            display: none;
            background: none;
            border: none;
            color: var(--light);
            font-size: 1.5rem;
            cursor: pointer;
            padding: 0.5rem;
        }

        .mobile-menu {
            display: none;
            position: fixed;
            top: 0;
            right: -100%;
            width: 80%;
            max-width: 400px;
            height: 100vh;
            background: var(--dark);
            padding: 2rem;
            transition: right 0.3s ease;
            z-index: 1001;
        }

        .mobile-menu.active {
            right: 0;
        }

        .mobile-menu-close {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: none;
            border: none;
            color: var(--light);
            font-size: 2rem;
            cursor: pointer;
        }

        .mobile-menu-links {
            margin-top: 3rem;
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .mobile-menu-links a {
            color: var(--light);
            text-decoration: none;
            font-size: 1.2rem;
            transition: color 0.3s;
        }

        .mobile-menu-links a:hover {
            color: var(--secondary);
        }

        .mobile-menu-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            background: rgba(0, 0, 0, 0.5);
            z-index: 1000;
        }

        .mobile-menu-overlay.active {
            display: block;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            background: linear-gradient(135deg, var(--dark) 0%, #2d1810 100%);
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            padding-top: 5rem;
        }

        .hero::before {
            content: "";
            position: absolute;
            width: 500px;
            height: 500px;
            background: radial-gradient(circle, var(--primary) 0%, transparent 70%);
            opacity: 0.1;
            top: -200px;
            right: -200px;
            animation: float 20s infinite ease-in-out;
        }

        .hero::after {
            content: "";
            position: absolute;
            width: 400px;
            height: 400px;
            background: radial-gradient(circle, var(--secondary) 0%, transparent 70%);
            opacity: 0.1;
            bottom: -150px;
            left: -150px;
            animation: float 25s infinite ease-in-out reverse;
        }

        @keyframes float {
            0%, 100% { transform: translate(0, 0) rotate(0deg); }
            33% { transform: translate(30px, -30px) rotate(120deg); }
            66% { transform: translate(-20px, 20px) rotate(240deg); }
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 1;
        }

        .hero-text h1 {
            font-size: 3.5rem;
            color: var(--light);
            margin-bottom: 1rem;
            line-height: 1.2;
        }

        .hero-text h1 span {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            filter: brightness(1.3);
        }

        .hero-text p {
            font-size: 1.3rem;
            color: rgba(255, 235, 205, 0.9);
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .hero-buttons {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .primary-button {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            padding: 1rem 2rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: transform 0.3s, box-shadow 0.3s;
            display: inline-block;
        }

        .primary-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(168, 36, 0, 0.4);
        }

        .secondary-button {
            background: transparent;
            color: var(--light);
            padding: 1rem 2rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            border: 2px solid var(--primary);
            transition: all 0.3s;
            display: inline-block;
        }

        .secondary-button:hover {
            background: var(--primary);
            transform: translateY(-3px);
        }

        .hero-visual {
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .virtual-world {
            width: 500px;
            height: 500px;
            position: relative;
            animation: rotate 30s infinite linear;
        }

        @keyframes rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .orbit {
            position: absolute;
            border: 2px dashed rgba(168, 36, 0, 0.3);
            border-radius: 50%;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
        }

        .orbit-1 { width: 300px; height: 300px; }
        .orbit-2 { width: 400px; height: 400px; }
        .orbit-3 { width: 500px; height: 500px; }

        .virtual-item {
            position: absolute;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.3);
            animation: counter-rotate 30s infinite linear;
        }

        @keyframes counter-rotate {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(-360deg); }
        }

        .item-1 { top: 0; left: 50%; transform: translateX(-50%); }
        .item-2 { top: 50%; right: 0; transform: translateY(-50%); }
        .item-3 { bottom: 0; left: 50%; transform: translateX(-50%); }
        .item-4 { top: 50%; left: 0; transform: translateY(-50%); }
        .item-5 { top: 20%; left: 20%; }
        .item-6 { bottom: 20%; right: 20%; }

        /* Features Section */
        .features {
            padding: 5rem 0;
            background: var(--cream);
        }

        .section-header {
            text-align: center;
            max-width: 700px;
            margin: 0 auto 4rem;
            padding: 0 2rem;
        }

        .section-header h2 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: var(--dark);
        }

        .section-header p {
            font-size: 1.2rem;
            color: var(--gray);
        }

        .features-grid {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: white;
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            transition: transform 0.3s, box-shadow 0.3s;
            position: relative;
            overflow: hidden;
        }

        .feature-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
        }

        .feature-icon {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--dark);
        }

        .feature-card p {
            color: var(--gray);
            line-height: 1.6;
        }

        /* Use Cases Section */
        .use-cases {
            padding: 5rem 0;
            background: linear-gradient(135deg, #fff5eb 0%, #ffe4d6 100%);
        }

        .use-case-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .use-case-card {
            background: white;
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 10px 40px rgba(168, 36, 0, 0.1);
            margin-bottom: 2rem;
        }

        .use-case-steps {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 2rem;
        }

        .step {
            text-align: center;
            position: relative;
        }

        .step-number {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.2rem;
            margin: 0 auto 1rem;
        }

        /* Target Audience */
        .audience {
            padding: 5rem 0;
            background: var(--dark);
            color: var(--light);
        }

        .audience-grid {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .audience-card {
            background: rgba(255, 235, 205, 0.05);
            border: 1px solid rgba(255, 235, 205, 0.1);
            padding: 2rem;
            border-radius: 16px;
            text-align: center;
            transition: all 0.3s;
        }

        .audience-card:hover {
            background: rgba(255, 235, 205, 0.1);
            transform: translateY(-5px);
            border-color: var(--primary);
        }

        .audience-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        /* CTA Section */
        .cta-section {
            padding: 5rem 0;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .cta-section::before {
            content: "";
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            top: -150px;
            right: -150px;
        }

        .cta-content {
            position: relative;
            z-index: 1;
            max-width: 700px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .cta-content h2 {
            font-size: 2.5rem;
            color: white;
            margin-bottom: 1rem;
        }

        .cta-content p {
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 2rem;
        }

        .cta-button-white {
            background: var(--light);
            color: var(--primary);
            padding: 1rem 2.5rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
            transition: transform 0.3s, box-shadow 0.3s;
            display: inline-block;
        }

        .cta-button-white:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
            background: white;
        }

        /* Footer */
        footer {
            background: var(--dark);
            color: var(--light);
            padding: 3rem 0;
            text-align: center;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Responsive Breakpoints */
        
        /* Large Tablets and Small Laptops */
        @media (max-width: 1024px) {
            .hero-text h1 {
                font-size: 3rem;
            }
            
            .hero-text p {
                font-size: 1.2rem;
            }
            
            .virtual-world {
                width: 400px;
                height: 400px;
            }
            
            .orbit-1 { width: 240px; height: 240px; }
            .orbit-2 { width: 320px; height: 320px; }
            .orbit-3 { width: 400px; height: 400px; }
        }

        /* Tablets */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }
            
            .mobile-menu-toggle {
                display: block;
            }

            .hero-content {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 3rem;
                padding: 0 1.5rem;
            }

            .hero-text h1 {
                font-size: 2.5rem;
            }
            
            .hero-text p {
                font-size: 1.1rem;
            }

            .virtual-world {
                width: 300px;
                height: 300px;
            }

            .orbit-1 { width: 180px; height: 180px; }
            .orbit-2 { width: 240px; height: 240px; }
            .orbit-3 { width: 300px; height: 300px; }

            .virtual-item {
                width: 40px;
                height: 40px;
                font-size: 1.2rem;
            }

            .hero-buttons {
                justify-content: center;
            }
            
            .primary-button,
            .secondary-button {
                padding: 0.8rem 1.5rem;
                font-size: 1rem;
            }
            
            .section-header h2 {
                font-size: 2rem;
            }
            
            .section-header p {
                font-size: 1.1rem;
            }
            
            .features-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .use-case-card {
                padding: 2rem;
            }
            
            .use-case-steps {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .audience-grid {
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
                gap: 1.5rem;
            }
            
            .cta-content h2 {
                font-size: 2rem;
            }
            
            .cta-content p {
                font-size: 1.1rem;
            }
        }

        /* Mobile Phones */
        @media (max-width: 480px) {
            .nav-container {
                padding: 0 1rem;
            }
            
            .logo {
                font-size: 1.3rem;
            }
            
            .logo::before {
                font-size: 1.5rem;
            }
            
            .hero {
                padding-top: 4rem;
            }
            
            .hero-text h1 {
                font-size: 2rem;
            }
            
            .hero-text p {
                font-size: 1rem;
                margin-bottom: 1.5rem;
            }
            
            .hero-buttons {
                flex-direction: column;
                align-items: center;
            }
            
            .primary-button,
            .secondary-button {
                width: 100%;
                max-width: 250px;
                text-align: center;
            }
            
            .virtual-world {
                width: 250px;
                height: 250px;
            }
            
            .orbit-1 { width: 150px; height: 150px; }
            .orbit-2 { width: 200px; height: 200px; }
            .orbit-3 { width: 250px; height: 250px; }
            
            .virtual-item {
                width: 35px;
                height: 35px;
                font-size: 1rem;
            }
            
            .features,
            .use-cases,
            .audience,
            .cta-section {
                padding: 3rem 0;
            }
            
            .section-header {
                margin-bottom: 2rem;
            }
            
            .section-header h2 {
                font-size: 1.8rem;
            }
            
            .section-header p {
                font-size: 1rem;
            }
            
            .features-grid,
            .use-case-container,
            .audience-grid,
            .footer-content {
                padding: 0 1rem;
            }
            
            .feature-card {
                padding: 1.5rem;
            }
            
            .feature-icon {
                width: 50px;
                height: 50px;
                font-size: 1.3rem;
            }
            
            .feature-card h3 {
                font-size: 1.3rem;
            }
            
            .use-case-card {
                padding: 1.5rem;
            }
            
            .use-case-card h3 {
                font-size: 1.5rem;
            }
            
            .audience-card {
                padding: 1.5rem;
            }
            
            .audience-icon {
                font-size: 2.5rem;
            }
            
            .cta-content h2 {
                font-size: 1.8rem;
            }
            
            .cta-content p {
                font-size: 1rem;
            }
            
            .cta-button-white {
                padding: 0.8rem 2rem;
                font-size: 1rem;
            }
            
            footer {
                padding: 2rem 0;
            }
            
            .footer-content p {
                font-size: 0.9rem;
            }
        }

        /* Small Mobile Phones */
        @media (max-width: 360px) {
            .hero-text h1 {
                font-size: 1.8rem;
            }
            
            .virtual-world {
                width: 200px;
                height: 200px;
            }
            
            .orbit-1 { width: 120px; height: 120px; }
            .orbit-2 { width: 160px; height: 160px; }
            .orbit-3 { width: 200px; height: 200px; }
            
            .virtual-item {
                width: 30px;
                height: 30px;
                font-size: 0.9rem;
            }
        }

        /* Landscape Mobile */
        @media (max-height: 600px) and (orientation: landscape) {
            .hero {
                min-height: auto;
                padding: 6rem 0 3rem;
            }
            
            .hero-content {
                grid-template-columns: 1fr 1fr;
                gap: 2rem;
            }
            
            .virtual-world {
                width: 250px;
                height: 250px;
            }
            
            .orbit-1 { width: 150px; height: 150px; }
            .orbit-2 { width: 200px; height: 200px; }
            .orbit-3 { width: 250px; height: 250px; }
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <div class="nav-container">
            <a href="#" class="logo">VirtuHub</a>
            <div class="nav-links">
                <a href="#features">Features</a>
                <a href="#use-cases">Use Cases</a>
                <a href="#audience">For Who</a>
                <a href="/community" class="cta-button">Get Started</a>
            </div>
            <button class="mobile-menu-toggle" onclick="toggleMobileMenu()">☰</button>
        </div>
    </nav>

    <!-- Mobile Menu -->
    <div class="mobile-menu-overlay" onclick="closeMobileMenu()"></div>
    <div class="mobile-menu">
        <button class="mobile-menu-close" onclick="closeMobileMenu()">×</button>
        <div class="mobile-menu-links">
            <a href="#features" onclick="closeMobileMenu()">Features</a>
            <a href="#use-cases" onclick="closeMobileMenu()">Use Cases</a>
            <a href="#audience" onclick="closeMobileMenu()">For Who</a>
            <a href="/community" class="cta-button" onclick="closeMobileMenu()">Get Started</a>
        </div>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <div class="hero-text">
                <h1>Build Your <span>Virtual Empire</span> in a Community That Gets You</h1>
                <p>Connect with like-minded people, create your own communities, and sell virtual products in the digital worlds of tomorrow.</p>
                <div class="hero-buttons">
                    <a href="/community" class="primary-button">Start Building Today</a>
                    <a href="#features" class="secondary-button">Explore Features</a>
                </div>
            </div>
            <div class="hero-visual">
                <div class="virtual-world">
                    <div class="orbit orbit-1"></div>
                    <div class="orbit orbit-2"></div>
                    <div class="orbit orbit-3"></div>
                    <div class="virtual-item item-1">📱</div>
                    <div class="virtual-item item-2">💻</div>
                    <div class="virtual-item item-3">🎮</div>
                    <div class="virtual-item item-4">👕</div>
                    <div class="virtual-item item-5">🛠️</div>
                    <div class="virtual-item item-6">🎨</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features" id="features">
        <div class="section-header">
            <h2>Your Gateway to the Virtual Economy</h2>
            <p>Everything you need to connect, create, and commerce in digital spaces</p>
        </div>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">🌐</div>
                <h3>Create Communities</h3>
                <p>Join or build your own communities based on shared thoughts, interests, and passions. Find your tribe in the digital realm.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🏪</div>
                <h3>Virtual Businesses</h3>
                <p>Start your own virtual business within any community. Register the company and set up shop and begin selling in minutes, not months.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">🎨</div>
                <h3>Digital Products</h3>
                <p>Design and sell virtual phones, computers, clothes, tools, and more for use in games, 3D worlds, and digital spaces.</p>
            </div>
        </div>
    </section>

    <!-- Use Cases Section -->
    <section class="use-cases" id="use-cases">
        <div class="section-header">
            <h2>See VirtuHub in Action</h2>
            <p>Real examples of how people are building virtual businesses</p>
        </div>
        <div class="use-case-container">
            <div class="use-case-card">
                <h3 style="color: var(--dark); font-size: 1.8rem; margin-bottom: 1rem;">The Gamer's Virtual Phone Shop</h3>
                <div class="use-case-steps">
                    <div class="step">
                        <div class="step-number">1</div>
                        <h4>Join Community</h4>
                        <p>A gamer joins a community of metaverse enthusiasts</p>
                    </div>
                    <div class="step">
                        <div class="step-number">2</div>
                        <h4>Open Shop</h4>
                        <p>Opens a virtual shop selling custom-designed phones</p>
                    </div>
                    <div class="step">
                        <div class="step-number">3</div>
                        <h4>Make Sales</h4>
                        <p>Other players buy and use the phones in their 3D worlds</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Target Audience Section -->
    <section class="audience" id="audience">
        <div class="section-header">
            <h2 style="color: var(--light);">Built for Digital Pioneers</h2>
            <p style="color: rgba(255, 235, 205, 0.8);">Join thousands who are already building in the virtual economy</p>
        </div>
        <div class="audience-grid">
            <div class="audience-card">
                <div class="audience-icon">🎨</div>
                <h3>Creators & Designers</h3>
                <p>Turn your digital creations into profitable virtual products</p>
            </div>
            <div class="audience-card">
                <div class="audience-icon">🎮</div>
                <h3>Gamers & Tech Lovers</h3>
                <p>Build and trade in the games and worlds you love</p>
            </div>
            <div class="audience-card">
                <div class="audience-icon">🤝</div>
                <h3>Community Seekers</h3>
                <p>Find genuine connections with people who think like you</p>
            </div>
            <div class="audience-card">
                <div class="audience-icon">🏢</div>
                <h3>Forward-Thinking Companies</h3>
                <p>Enter the metaverse with your own virtual presence</p>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
        <div class="cta-content">
            <h2>Ready to Build Your Virtual Future?</h2>
            <p>Join VirtuHub today and start connecting, creating, and selling in the digital economy of tomorrow.</p>
            <a href="/community" class="cta-button-white">Get Started Free</a>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-content">
            <p>&copy; 2024 VirtuHub. All rights reserved. | Building the virtual economy, one community at a time.</p>
        </div>
    </footer>

    <script>
        // Mobile menu functions
        function toggleMobileMenu() {
            const menu = document.querySelector('.mobile-menu');
            const overlay = document.querySelector('.mobile-menu-overlay');
            menu.classList.add('active');
            overlay.classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        function closeMobileMenu() {
            const menu = document.querySelector('.mobile-menu');
            const overlay = document.querySelector('.mobile-menu-overlay');
            menu.classList.remove('active');
            overlay.classList.remove('active');
            document.body.style.overflow = 'auto';
        }

        // Smooth scrolling for navigation links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Add animation on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        };

        const observer = new IntersectionObserver(function(entries) {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observe feature cards and use case cards
        document.querySelectorAll('.feature-card, .use-case-card, .audience-card').forEach(el => {
            el.style.opacity = '0';
            el.style.transform = 'translateY(20px)';
            el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
            observer.observe(el);
        });

        // Responsive navigation scroll behavior
        let lastScroll = 0;
        const nav = document.querySelector('nav');
        
        window.addEventListener('scroll', () => {
            const currentScroll = window.pageYOffset;
            
            if (currentScroll <= 0) {
                nav.style.transform = 'translateY(0)';
                return;
            }
            
            if (currentScroll > lastScroll && !nav.classList.contains('scroll-down')) {
                // Scrolling down
                nav.style.transform = 'translateY(-100%)';
            } else if (currentScroll < lastScroll && nav.classList.contains('scroll-down')) {
                // Scrolling up
                nav.style.transform = 'translateY(0)';
            }
            
            lastScroll = currentScroll;
        });

        // Prevent body scroll when mobile menu is open
        const mobileMenu = document.querySelector('.mobile-menu');
        const mobileOverlay = document.querySelector('.mobile-menu-overlay');
        
        // Handle window resize
        let resizeTimer;
        window.addEventListener('resize', () => {
            clearTimeout(resizeTimer);
            resizeTimer = setTimeout(() => {
                if (window.innerWidth > 768) {
                    closeMobileMenu();
                }
            }, 250);
        });

        // Touch swipe to close mobile menu
        let touchStartX = null;
        
        mobileMenu.addEventListener('touchstart', (e) => {
            touchStartX = e.touches[0].clientX;
        });
        
        mobileMenu.addEventListener('touchmove', (e) => {
            if (!touchStartX) return;
            
            const touchEndX = e.touches[0].clientX;
            const diff = touchEndX - touchStartX;
            
            if (diff > 50) {
                closeMobileMenu();
                touchStartX = null;
            }
        });
    </script>
</body>
</html>