<%- layout('/layouts/boilerplate.ejs') %>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VirtuMart Pro - Professional Virtual Asset Marketplace</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');
        
        :root {
            --primary: #A82400;
            --white: #ffffff;
            
            /* Professional color derivatives */
            --primary-dark: #8B1E00;
            --primary-light: #C72E00;
            --primary-rgb: 168, 36, 0;
            
            /* Background system */
            --bg-primary: var(--white);
            --bg-secondary: #fafafa;
            --bg-elevated: #ffffff;
            --bg-accent: var(--primary);
            
            /* Surface system */
            --surface: rgba(var(--primary-rgb), 0.03);
            --surface-hover: rgba(var(--primary-rgb), 0.06);
            --surface-active: rgba(var(--primary-rgb), 0.08);
            
            /* Text system */
            --text-primary: #1a1a1a;
            --text-secondary: #6b7280;
            --text-tertiary: #9ca3af;
            --text-inverse: var(--white);
            --text-accent: var(--primary);
            
            /* Border system */
            --border-light: #f3f4f6;
            --border-default: #e5e7eb;
            --border-strong: #d1d5db;
            --border-accent: var(--primary);
            
            /* Shadow system */
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
            --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
            --shadow-accent: 0 4px 14px 0 rgba(var(--primary-rgb), 0.25);
            --shadow-accent-lg: 0 10px 25px 0 rgba(var(--primary-rgb), 0.3);
            
            /* Professional gradients */
            --gradient-primary: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
            --gradient-surface: linear-gradient(to bottom, var(--white), var(--bg-secondary));
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* Professional Navigation */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px) saturate(1.8);
            border-bottom: 1px solid var(--border-light);
            z-index: 1000;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .navbar.scrolled {
            background: rgba(255, 255, 255, 0.98);
            box-shadow: var(--shadow-md);
        }

        .navbar-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
            color: var(--text-primary);
            font-weight: 700;
            font-size: 1.5rem;
            letter-spacing: -0.025em;
        }

        .brand-logo {
            width: 40px;
            height: 40px;
            background: var(--gradient-primary);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 1.25rem;
            font-weight: 700;
            box-shadow: var(--shadow-accent);
        }

        .nav-menu {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            font-weight: 500;
            font-size: 0.95rem;
            position: relative;
            transition: all 0.2s ease;
            padding: 0.75rem 0;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--primary);
            transition: width 0.3s ease;
        }

        .nav-link:hover,
        .nav-link.active {
            color: var(--text-accent);
        }

        .nav-link:hover::after,
        .nav-link.active::after {
            width: 100%;
        }

        .nav-actions {
            display: flex;
            gap: 1rem;
            align-items: center;
        }

        .search-box {
            position: relative;
            width: 300px;
        }

        .search-input {
            width: 100%;
            padding: 0.75rem 1rem 0.75rem 2.5rem;
            border: 1px solid var(--border-default);
            border-radius: 10px;
            background: var(--bg-primary);
            color: var(--text-primary);
            font-size: 0.9rem;
            outline: none;
            transition: all 0.2s ease;
        }

        .search-input:focus {
            border-color: var(--border-accent);
            box-shadow: 0 0 0 3px rgba(var(--primary-rgb), 0.1);
        }

        .search-icon {
            position: absolute;
            left: 0.75rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-tertiary);
            font-size: 1rem;
        }

        .btn {
            padding: 0.75rem 1.5rem;
            border-radius: 10px;
            font-weight: 600;
            font-size: 0.9rem;
            cursor: pointer;
            border: none;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
        }

        .btn-primary {
            background: var(--gradient-primary);
            color: var(--white);
            box-shadow: var(--shadow-accent);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-accent-lg);
        }

        .btn-secondary {
            background: var(--bg-primary);
            color: var(--text-primary);
            border: 1px solid var(--border-default);
        }

        .btn-secondary:hover {
            border-color: var(--border-accent);
            background: var(--surface);
        }

        .cart-btn {
            position: relative;
        }

        .cart-badge {
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--primary);
            color: var(--white);
            border-radius: 50%;
            width: 20px;
            height: 20px;
            font-size: 0.75rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 2px solid var(--white);
        }

        /* Hero Section */
        .hero {
            padding: 5rem 2rem 6rem;
            background: var(--gradient-surface);
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 50%;
            height: 100%;
            background: linear-gradient(135deg, rgba(var(--primary-rgb), 0.05) 0%, transparent 70%);
            z-index: 1;
        }

        .hero-container {
            max-width: 1400px;
            margin: 18px auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .hero-content {
            max-width: 600px;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: var(--surface);
            border: 1px solid var(--border-light);
            border-radius: 100px;
            padding: 0.5rem 1rem;
            font-size: 0.85rem;
            color: var(--text-accent);
            margin-bottom: 2rem;
            font-weight: 500;
        }

        .hero-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 800;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            color: var(--text-primary);
            letter-spacing: -0.025em;
        }

        .hero-title .accent {
            color: var(--text-accent);
        }

        .hero-subtitle {
            font-size: 1.2rem;
            color: var(--text-secondary);
            margin-bottom: 2.5rem;
            line-height: 1.6;
        }

        .hero-actions {
            display: flex;
            gap: 1rem;
            margin-bottom: 3rem;
        }

        .hero-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
        }

        .stat {
            text-align: left;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 800;
            color: var(--text-primary);
            line-height: 1;
            margin-bottom: 0.25rem;
        }

        .stat-label {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        .hero-visual {
            position: relative;
        }

        .hero-image {
            width: 100%;
            height: 500px;
            background: var(--white);
            border-radius: 20px;
            border: 1px solid var(--border-light);
            box-shadow: var(--shadow-xl);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .product-showcase {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
            width: 80%;
            height: 80%;
        }

        .showcase-item {
            background: var(--gradient-primary);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--white);
            font-size: 2rem;
            box-shadow: var(--shadow-md);
            transition: transform 0.3s ease;
        }

        .showcase-item:hover {
            transform: scale(1.05);
        }

        /* Professional Categories */
        .categories {
            padding: 6rem 2rem;
            background: var(--bg-primary);
        }

        .section-container {
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-header {
            text-align: center;
            margin-bottom: 4rem;
        }

        .section-subtitle {
            color: var(--text-accent);
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            font-size: 0.85rem;
            margin-bottom: 1rem;
        }

        .section-title {
            font-size: clamp(2rem, 4vw, 3rem);
            font-weight: 800;
            color: var(--text-primary);
            margin-bottom: 1rem;
            letter-spacing: -0.025em;
        }

        .section-description {
            font-size: 1.1rem;
            color: var(--text-secondary);
            max-width: 600px;
            margin: 0 auto;
        }

        .categories-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }

        .category-card {
            background: var(--bg-elevated);
            border: 1px solid var(--border-light);
            border-radius: 16px;
            padding: 2rem;
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .category-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, var(--surface), transparent);
            transition: left 0.5s ease;
        }

        .category-card:hover::before {
            left: 100%;
        }

        .category-card:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-xl);
            border-color: var(--border-accent);
        }

        .category-icon {
            width: 60px;
            height: 60px;
            background: var(--gradient-primary);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: var(--white);
            margin-bottom: 1.5rem;
            box-shadow: var(--shadow-accent);
        }

        .category-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.5rem;
        }

        .category-description {
            color: var(--text-secondary);
            margin-bottom: 1.5rem;
            line-height: 1.5;
        }

        .category-meta {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.85rem;
            color: var(--text-tertiary);
        }

        /* Products Section */
        .products {
            padding: 6rem 2rem;
            background: var(--bg-secondary);
        }

        .products-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
        }

        .filter-tabs {
            display: flex;
            gap: 0.5rem;
            background: var(--bg-elevated);
            padding: 0.5rem;
            border-radius: 12px;
            border: 1px solid var(--border-light);
        }

        .filter-tab {
            padding: 0.6rem 1.2rem;
            background: transparent;
            border: none;
            border-radius: 8px;
            color: var(--text-secondary);
            font-weight: 500;
            cursor: pointer;
            transition: all 0.2s ease;
            font-size: 0.9rem;
        }

        .filter-tab.active,
        .filter-tab:hover {
            background: var(--gradient-primary);
            color: var(--white);
            box-shadow: var(--shadow-sm);
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2rem;
        }

        .product-card {
            background: var(--bg-elevated);
            border: 1px solid var(--border-light);
            border-radius: 16px;
            overflow: hidden;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .product-card:hover {
            transform: translateY(-6px);
            box-shadow: var(--shadow-xl);
            border-color: var(--border-accent);
        }

        .product-image {
            height: 200px;
            background: var(--gradient-primary);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            color: var(--white);
            position: relative;
        }

        .product-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            padding: 0.25rem 0.75rem;
            border-radius: 100px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .badge-legendary {
            background: linear-gradient(135deg, #fbbf24, #f59e0b);
            color: var(--white);
        }

        .badge-epic {
            background: linear-gradient(135deg, #a855f7, #9333ea);
            color: var(--white);
        }

        .badge-rare {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: var(--white);
        }

        .badge-enterprise {
            background: linear-gradient(135deg, #ef4444, #dc2626);
            color: var(--white);
        }

        .badge-professional {
            background: linear-gradient(135deg, #10b981, #059669);
            color: var(--white);
        }

        .product-info {
            padding: 1.5rem;
        }

        .product-category {
            font-size: 0.8rem;
            color: var(--text-tertiary);
            text-transform: uppercase;
            font-weight: 600;
            letter-spacing: 0.1em;
            margin-bottom: 0.5rem;
        }

        .product-title {
            font-size: 1.1rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 0.75rem;
            line-height: 1.3;
        }

        .product-description {
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.5;
            margin-bottom: 1.5rem;
        }

        .product-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .product-price {
            display: flex;
            align-items: baseline;
            gap: 0.5rem;
        }

        .price-current {
            font-size: 1.25rem;
            font-weight: 800;
            color: var(--text-primary);
        }

        .price-original {
            font-size: 0.9rem;
            color: var(--text-tertiary);
            text-decoration: line-through;
        }

        .product-actions {
            display: flex;
            gap: 0.5rem;
        }

        .btn-icon {
            width: 36px;
            height: 36px;
            border-radius: 8px;
            border: 1px solid var(--border-default);
            background: var(--bg-primary);
            color: var(--text-secondary);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.2s ease;
        }

        .btn-icon:hover {
            border-color: var(--border-accent);
            color: var(--text-accent);
            background: var(--surface);
        }

        .btn-small {
            padding: 0.5rem 1rem;
            font-size: 0.85rem;
        }

        /* Floating Elements */
        .floating-action {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            z-index: 900;
        }

        .fab {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            background: var(--gradient-primary);
            color: var(--white);
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.25rem;
            box-shadow: var(--shadow-accent-lg);
            transition: all 0.3s ease;
        }

        .fab:hover {
            transform: scale(1.1);
            box-shadow: 0 12px 30px 0 rgba(var(--primary-rgb), 0.4);
        }

        /* Responsive Design - Enhanced Mobile Support */
        @media (max-width: 1200px) {
            .navbar-container {
                padding: 1rem;
            }
            
            .search-box {
                width: 200px;
            }
            
            .hero-container {
                gap: 3rem;
            }
        }

        @media (max-width: 1024px) {
            .hero-container {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 3rem;
            }
            
            .nav-menu {
                display: none;
            }
            
            .search-box {
                width: 180px;
            }
            
            .categories-grid {
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            }
            
            .products-grid {
                grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            }
        }

        @media (max-width: 768px) {
            .navbar-container {
                padding: 0.75rem 1rem;
            }
            
            .brand {
                font-size: 1.25rem;
            }
            
            .brand-logo {
                width: 36px;
                height: 36px;
                font-size: 1.1rem;
            }
            
            .nav-actions {
                gap: 0.5rem;
            }
            
            .search-box {
                display: none;
            }
            
            .btn {
                padding: 0.6rem 1rem;
                font-size: 0.85rem;
            }
            
            .hero {
                padding: 5rem 1rem 4rem;
            }
            
            .hero-title {
                font-size: clamp(2rem, 8vw, 3rem);
                margin-bottom: 1rem;
            }
            
            .hero-subtitle {
                font-size: 1rem;
                margin-bottom: 2rem;
            }
            
            .hero-actions {
                flex-direction: column;
                align-items: center;
                gap: 0.75rem;
                margin-bottom: 2rem;
            }
            
            .hero-actions .btn {
                width: 100%;
                max-width: 280px;
                justify-content: center;
            }
            
            .hero-stats {
                grid-template-columns: repeat(3, 1fr);
                gap: 1rem;
                text-align: center;
            }
            
            .stat-number {
                font-size: 1.5rem;
            }
            
            .stat-label {
                font-size: 0.8rem;
            }
            
            .hero-image {
                height: 300px;
            }
            
            .product-showcase {
                grid-template-columns: repeat(2, 1fr);
                gap: 0.75rem;
            }
            
            .showcase-item {
                font-size: 1.5rem;
            }
            
            .categories,
            .products {
                padding: 4rem 1rem;
            }
            
            .section-header {
                margin-bottom: 3rem;
            }
            
            .section-title {
                font-size: clamp(1.75rem, 6vw, 2.5rem);
            }
            
            .section-description {
                font-size: 1rem;
                padding: 0 1rem;
            }
            
            .categories-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .category-card {
                padding: 1.5rem;
            }
            
            .category-icon {
                width: 50px;
                height: 50px;
                font-size: 1.25rem;
                margin-bottom: 1rem;
            }
            
            .category-title {
                font-size: 1.1rem;
            }
            
            .category-description {
                font-size: 0.9rem;
                margin-bottom: 1rem;
            }
            
            .category-meta {
                font-size: 0.8rem;
                flex-direction: column;
                gap: 0.25rem;
                align-items: flex-start;
            }
            
            .products-header {
                flex-direction: column;
                gap: 1.5rem;
                align-items: stretch;
            }
            
            .filter-tabs {
                justify-content: center;
                flex-wrap: wrap;
                gap: 0.25rem;
                padding: 0.25rem;
            }
            
            .filter-tab {
                padding: 0.5rem 0.8rem;
                font-size: 0.8rem;
                flex: 1;
                min-width: auto;
                text-align: center;
            }
            
            .products-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .product-card {
                max-width: 100%;
            }
            
            .product-image {
                height: 180px;
                font-size: 2.5rem;
            }
            
            .product-info {
                padding: 1.25rem;
            }
            
            .product-footer {
                flex-direction: column;
                gap: 1rem;
                align-items: stretch;
            }
            
            .product-actions {
                justify-content: space-between;
            }
            
            .btn-small {
                flex: 1;
                justify-content: center;
            }
            
            .floating-action {
                bottom: 1rem;
                right: 1rem;
                gap: 0.75rem;
            }
            
            .fab {
                width: 48px;
                height: 48px;
                font-size: 1.1rem;
            }
        }

        @media (max-width: 480px) {
            .navbar-container {
                padding: 0.5rem;
            }
            
            .brand {
                font-size: 1.1rem;
            }
            
            .brand-logo {
                width: 32px;
                height: 32px;
                font-size: 1rem;
            }
            
            .hero {
                padding: 4rem 0.75rem 3rem;
            }
            
            .hero-badge {
                padding: 0.4rem 0.8rem;
                font-size: 0.8rem;
            }
            
            .hero-title {
                font-size: clamp(1.75rem, 10vw, 2.5rem);
            }
            
            .hero-subtitle {
                font-size: 0.95rem;
            }
            
            .hero-stats {
                gap: 0.75rem;
            }
            
            .stat-number {
                font-size: 1.25rem;
            }
            
            .stat-label {
                font-size: 0.75rem;
            }
            
            .categories,
            .products {
                padding: 3rem 0.75rem;
            }
            
            .section-header {
                margin-bottom: 2rem;
            }
            
            .section-description {
                padding: 0;
            }
            
            .category-card,
            .product-info {
                padding: 1rem;
            }
            
            .filter-tabs {
                padding: 0.2rem;
                gap: 0.1rem;
            }
            
            .filter-tab {
                padding: 0.4rem 0.6rem;
                font-size: 0.75rem;
            }
            
            .floating-action {
                bottom: 0.75rem;
                right: 0.75rem;
            }
            
            .fab {
                width: 44px;
                height: 44px;
                font-size: 1rem;
            }
        }

        @media (max-width: 360px) {
            .hero-actions .btn {
                max-width: 100%;
            }
            
            .product-showcase {
                grid-template-columns: 1fr 1fr;
                width: 90%;
            }
            
            .showcase-item {
                font-size: 1.25rem;
            }
            
            .category-meta {
                font-size: 0.75rem;
            }
            
            .product-title {
                font-size: 1rem;
            }
            
            .product-description {
                font-size: 0.85rem;
            }
            
            .price-current {
                font-size: 1.1rem;
            }
        }

        /* Loading States */
        .loading {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 200px;
        }

        .loader {
            width: 40px;
            height: 40px;
            border: 3px solid var(--border-light);
            border-left: 3px solid var(--primary);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Animations */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            animation: fadeInUp 0.6s ease forwards;
        }

        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .slide-in {
            opacity: 0;
            transform: translateX(-30px);
            animation: slideInLeft 0.6s ease forwards;
        }

        @keyframes slideInLeft {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
    </style>
</head>
<body>
    <!-- Professional Navigation -->
    <!-- <nav class="navbar" id="navbar">
        <div class="navbar-container">
            <a href="/" class="brand">
                <div class="brand-logo">VM</div>
                <span>VirtuMart Pro</span>
            </a>
            
            <div class="nav-menu">
                <a href="#" class="nav-link active">Marketplace</a>
                <a href="#" class="nav-link">Categories</a>
                <a href="#" class="nav-link">Collections</a>
                <a href="#" class="nav-link">Creators</a>
                <a href="#" class="nav-link">Enterprise</a>
            </div>
            
            <div class="nav-actions">
                <div class="search-box">
                    <span class="search-icon">🔍</span>
                    <input type="text" class="search-input" placeholder="Search professional virtual assets...">
                </div>
                <button class="btn btn-primary cart-btn" onclick="toggleCart()">
                    <span>Cart</span>
                    <div class="cart-badge">3</div>
                </button>
            </div>
        </div>
    </nav> -->

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-container">
            <div class="hero-content slide-in">
                <div class="hero-badge">
                    <span>🚀</span>
                    <span>Professional Marketplace</span>
                </div>
                
                <h1 class="hero-title">
                    Enterprise-Grade <span class="accent">Virtual Assets</span> for Professionals
                </h1>
                
                <p class="hero-subtitle">
                    Access premium virtual tools, enterprise solutions, and professional-grade digital assets designed for business applications and advanced virtual environments.
                </p>
                
                <div class="hero-actions">
                    <a href="#products" class="btn btn-primary">
                        <span>Explore Marketplace</span>
                        <span>→</span>
                    </a>
                    <a href="#enterprise" class="btn btn-secondary">
                        <span>Enterprise Solutions</span>
                    </a>
                </div>
                
                <div class="hero-stats">
                    <div class="stat">
                        <div class="stat-number">50K+</div>
                        <div class="stat-label">Professional Assets</div>
                    </div>
                    <div class="stat">
                        <div class="stat-number">1,200+</div>
                        <div class="stat-label">Verified Creators</div>
                    </div>
                    <div class="stat">
                        <div class="stat-number">99.9%</div>
                        <div class="stat-label">Uptime SLA</div>
                    </div>
                </div>
            </div>
            
            <div class="hero-visual fade-in">
                <div class="hero-image">
                    <div class="product-showcase">
                        <div class="showcase-item">💼</div>
                        <div class="showcase-item">🔧</div>
                        <div class="showcase-item">🏢</div>
                        <div class="showcase-item">📊</div>
                        <div class="showcase-item">⚙️</div>
                        <div class="showcase-item">🎯</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Categories Section -->
    <section class="categories" id="categories">
        <div class="section-container">
            <div class="section-header">
                <div class="section-subtitle">Professional Categories</div>
                <h2 class="section-title">Business-Ready Solutions</h2>
                <p class="section-description">
                    Discover our comprehensive range of professional virtual assets, enterprise tools, and business solutions designed for modern organizations.
                </p>
            </div>
            
            <div class="categories-grid">
                <div class="category-card fade-in" onclick="filterProducts('business')">
                    <div class="category-icon">💼</div>
                    <h3 class="category-title">Business Tools</h3>
                    <p class="category-description">Professional productivity software, project management tools, and enterprise applications for virtual workspaces.</p>
                    <div class="category-meta">
                        <span>2,400+ Items</span>
                        <span>Enterprise Ready</span>
                    </div>
                </div>
                
                <div class="category-card fade-in" onclick="filterProducts('development')">
                    <div class="category-icon">🔧</div>
                    <h3 class="category-title">Development Platforms</h3>
                    <p class="category-description">Advanced development environments, coding tools, and professional IDEs for virtual development.</p>
                    <div class="category-meta">
                        <span>1,800+ Items</span>
                        <span>Developer Focused</span>
                    </div>
                </div>
                
                <div class="category-card fade-in" onclick="filterProducts('infrastructure')">
                    <div class="category-icon">🏢</div>
                    <h3 class="category-title">Virtual Infrastructure</h3>
                    <p class="category-description">Scalable virtual buildings, data centers, and enterprise-grade infrastructure components.</p>
                    <div class="category-meta">
                        <span>950+ Items</span>
                        <span>Scalable Solutions</span>
                    </div>
                </div>
                
                <div class="category-card fade-in" onclick="filterProducts('analytics')">
                    <div class="category-icon">📊</div>
                    <h3 class="category-title">Analytics & Insights</h3>
                    <p class="category-description">Business intelligence tools, data visualization platforms, and analytics dashboards.</p>
                    <div class="category-meta">
                        <span>1,200+ Items</span>
                        <span>Data Driven</span>
                    </div>
                </div>
                
                <div class="category-card fade-in" onclick="filterProducts('automation')">
                    <div class="category-icon">⚙️</div>
                    <h3 class="category-title">Process Automation</h3>
                    <p class="category-description">Workflow automation tools, robotic process automation, and efficiency optimization systems.</p>
                    <div class="category-meta">
                        <span>800+ Items</span>
                        <span>Automation Ready</span>
                    </div>
                </div>
                
                <div class="category-card fade-in" onclick="filterProducts('security')">
                    <div class="category-icon">🛡️</div>
                    <h3 class="category-title">Security Solutions</h3>
                    <p class="category-description">Enterprise security tools, encryption systems, and cybersecurity frameworks for virtual environments.</p>
                    <div class="category-meta">
                        <span>600+ Items</span>
                        <span>Security First</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Products Section -->
    <section class="products" id="products">
        <div class="section-container">
            <div class="products-header">
                <div>
                    <div class="section-subtitle">Featured Products</div>
                    <h2 class="section-title">Professional Virtual Assets</h2>
                </div>
                
                <div class="filter-tabs">
                    <button class="filter-tab active" onclick="filterProducts('all')">All Products</button>
                    <button class="filter-tab" onclick="filterProducts('trending')">Trending</button>
                    <button class="filter-tab" onclick="filterProducts('enterprise')">Enterprise</button>
                    <button class="filter-tab" onclick="filterProducts('new')">New Arrivals</button>
                    <button class="filter-tab" onclick="filterProducts('featured')">Featured</button>
                </div>
            </div>

            <div class="products-grid" id="productsGrid">
                <!-- Products will be loaded here -->
            </div>
        </div>
    </section>

    <!-- Floating Action Buttons -->
    <div class="floating-action">
        <button class="fab" onclick="scrollToTop()" title="Back to top">↑</button>
        <button class="fab" onclick="openSupport()" title="Support">💬</button>
        <button class="fab" onclick="toggleWishlist()" title="Wishlist">♡</button>
    </div>

    <script>
        // Professional product data
        const products = [
            {
                id: 1,
                title: "Enterprise Communication Hub",
                category: "business",
                description: "Comprehensive virtual communication platform with video conferencing, collaboration tools, and enterprise-grade security features.",
                price: 4999,
                originalPrice: 6999,
                rarity: "enterprise",
                icon: "💼",
                tags: ["trending", "enterprise", "featured"],
                rating: 4.9,
                reviews: 1247
            },
            {
                id: 2,
                title: "Professional Development Suite",
                category: "development",
                description: "Complete integrated development environment with advanced debugging, version control, and team collaboration capabilities.",
                price: 3499,
                originalPrice: null,
                rarity: "professional",
                icon: "🔧",
                tags: ["trending", "new"],
                rating: 4.8,
                reviews: 892
            },
            {
                id: 3,
                title: "Virtual Data Center Complex",
                category: "infrastructure",
                description: "Scalable virtual data center with cloud integration, load balancing, and enterprise-grade redundancy systems.",
                price: 15999,
                originalPrice: 19999,
                rarity: "enterprise",
                icon: "🏢",
                tags: ["enterprise", "featured"],
                rating: 4.9,
                reviews: 456
            },
            {
                id: 4,
                title: "Advanced Analytics Platform",
                category: "analytics",
                description: "Real-time business intelligence platform with machine learning capabilities and predictive analytics engine.",
                price: 7999,
                originalPrice: null,
                rarity: "professional",
                icon: "📊",
                tags: ["new", "trending"],
                rating: 4.7,
                reviews: 634
            },
            {
                id: 5,
                title: "Workflow Automation Engine",
                category: "automation",
                description: "Intelligent process automation system with AI-driven workflow optimization and cross-platform integration.",
                price: 5999,
                originalPrice: 7999,
                rarity: "enterprise",
                icon: "⚙️",
                tags: ["featured", "automation"],
                rating: 4.8,
                reviews: 521
            },
            {
                id: 6,
                title: "Enterprise Security Framework",
                category: "security",
                description: "Comprehensive cybersecurity solution with threat detection, incident response, and compliance management tools.",
                price: 12999,
                originalPrice: null,
                rarity: "enterprise",
                icon: "🛡️",
                tags: ["security", "enterprise"],
                rating: 4.9,
                reviews: 389
            },
            {
                id: 7,
                title: "Virtual Meeting Arena",
                category: "business",
                description: "Immersive virtual conference space with interactive presentations, networking zones, and event management tools.",
                price: 2999,
                originalPrice: 3999,
                rarity: "professional",
                icon: "🎯",
                tags: ["new", "trending"],
                rating: 4.6,
                reviews: 743
            },
            {
                id: 8,
                title: "Cloud Integration Platform",
                category: "development",
                description: "Multi-cloud integration platform with API management, microservices architecture, and serverless computing support.",
                price: 8999,
                originalPrice: null,
                rarity: "enterprise",
                icon: "☁️",
                tags: ["enterprise", "featured"],
                rating: 4.8,
                reviews: 312
            },
            {
                id: 9,
                title: "Professional Training Simulator",
                category: "business",
                description: "Virtual reality training platform with scenario modeling, skill assessment, and performance analytics for enterprise use.",
                price: 6999,
                originalPrice: 8999,
                rarity: "professional",
                icon: "🎓",
                tags: ["training", "new"],
                rating: 4.7,
                reviews: 567
            },
            {
                id: 10,
                title: "Enterprise Resource Planner",
                category: "business",
                description: "Comprehensive ERP system with financial management, supply chain optimization, and human resources integration.",
                price: 18999,
                originalPrice: null,
                rarity: "enterprise",
                icon: "📈",
                tags: ["enterprise", "featured"],
                rating: 4.9,
                reviews: 234
            }
        ];

        let currentFilter = 'all';
        let cart = [];
        let wishlist = [];

        // Initialize the application
        document.addEventListener('DOMContentLoaded', function() {
            displayProducts(products);
            updateCartCount();
            initializeScrollEffects();
            initializeAnimations();
        });

        // Filter products by category or tag
        function filterProducts(filter) {
            currentFilter = filter;
            
            // Update active filter tab
            document.querySelectorAll('.filter-tab').forEach(tab => {
                tab.classList.remove('active');
            });
            event.target.classList.add('active');

            let filteredProducts = products;
            
            if (filter !== 'all') {
                filteredProducts = products.filter(product => 
                    product.category === filter || 
                    product.tags.includes(filter) ||
                    product.rarity === filter
                );
            }

            displayProducts(filteredProducts);
        }

        // Display products in grid
        function displayProducts(productsToShow) {
            const grid = document.getElementById('productsGrid');
            
            // Show loading state
            grid.innerHTML = '<div class="loading"><div class="loader"></div></div>';
            
            setTimeout(() => {
                grid.innerHTML = productsToShow.map((product, index) => `
                    <div class="product-card fade-in" onclick="viewProduct(${product.id})" style="animation-delay: ${index * 100}ms">
                        <div class="product-image">
                            ${product.icon}
                            <div class="product-badge badge-${product.rarity}">${product.rarity}</div>
                        </div>
                        <div class="product-info">
                            <div class="product-category">${product.category}</div>
                            <h3 class="product-title">${product.title}</h3>
                            <p class="product-description">${product.description}</p>
                            <div class="product-footer">
                                <div class="product-price">
                                    <span class="price-current">${product.price.toLocaleString()}</span>
                                    ${product.originalPrice ? `<span class="price-original">${product.originalPrice.toLocaleString()}</span>` : ''}
                                </div>
                                <div class="product-actions">
                                    <button class="btn-icon" onclick="toggleWishlistItem(${product.id}, event)" title="Add to wishlist">
                                        ${wishlist.includes(product.id) ? '♥' : '♡'}
                                    </button>
                                    <button class="btn btn-primary btn-small" onclick="addToCart(${product.id}, event)">
                                        Add to Cart
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>
                `).join('');
            }, 600);
        }

        // Add to cart functionality
        function addToCart(productId, event) {
            event.stopPropagation();
            const product = products.find(p => p.id === productId);
            
            if (!cart.find(item => item.id === productId)) {
                cart.push(product);
                updateCartCount();
                
                // Show success feedback
                const button = event.target;
                const originalText = button.innerHTML;
                button.innerHTML = '✓ Added';
                button.style.background = 'linear-gradient(135deg, #10b981, #059669)';
                
                setTimeout(() => {
                    button.innerHTML = originalText;
                    button.style.background = 'var(--gradient-primary)';
                }, 2000);
                
                showNotification('Product added to cart successfully!', 'success');
            } else {
                showNotification('Product is already in your cart!', 'warning');
            }
        }

        // Toggle wishlist item
        function toggleWishlistItem(productId, event) {
            event.stopPropagation();
            const button = event.target;
            
            if (wishlist.includes(productId)) {
                wishlist = wishlist.filter(id => id !== productId);
                button.textContent = '♡';
                button.style.color = 'var(--text-secondary)';
                showNotification('Removed from wishlist', 'info');
            } else {
                wishlist.push(productId);
                button.textContent = '♥';
                button.style.color = '#ef4444';
                showNotification('Added to wishlist!', 'success');
            }
        }

        // Update cart count display
        function updateCartCount() {
            const cartBadge = document.querySelector('.cart-badge');
            if (cartBadge) {
                cartBadge.textContent = cart.length;
                cartBadge.style.display = cart.length === 0 ? 'none' : 'flex';
            }
        }

        // View product details modal
        function viewProduct(productId) {
            const product = products.find(p => p.id === productId);
            const modal = document.createElement('div');
            modal.style.cssText = `
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0, 0, 0, 0.5);
                z-index: 2000;
                display: flex;
                align-items: center;
                justify-content: center;
                padding: 2rem;
                backdrop-filter: blur(8px);
            `;
            
            modal.innerHTML = `
                <div style="background: var(--bg-elevated); border-radius: 20px; padding: 2rem; max-width: 600px; width: 100%; border: 1px solid var(--border-light); box-shadow: var(--shadow-xl);" onclick="event.stopPropagation()">
                    <div style="display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 1.5rem;">
                        <h2 style="color: var(--text-primary); font-size: 1.5rem; font-weight: 700;">${product.title}</h2>
                        <button onclick="this.closest('[style*=\"fixed\"]').remove()" style="background: none; border: none; font-size: 1.5rem; color: var(--text-tertiary); cursor: pointer;">×</button>
                    </div>
                    <div style="text-align: center; margin: 2rem 0;">
                        <div style="font-size: 4rem; margin-bottom: 1rem;">${product.icon}</div>
                        <span class="product-badge badge-${product.rarity}" style="display: inline-block;">${product.rarity}</span>
                    </div>
                    <p style="color: var(--text-secondary); margin-bottom: 1.5rem; line-height: 1.6;">${product.description}</p>
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; padding: 1rem; background: var(--surface); border-radius: 12px;">
                        <div>
                            <div style="font-size: 2rem; font-weight: 800; color: var(--text-primary);">${product.price.toLocaleString()}</div>
                            ${product.originalPrice ? `<div style="font-size: 1rem; color: var(--text-tertiary); text-decoration: line-through;">${product.originalPrice.toLocaleString()}</div>` : ''}
                        </div>
                        <div style="text-align: right;">
                            <div style="color: var(--text-secondary); font-size: 0.9rem;">⭐ ${product.rating} (${product.reviews} reviews)</div>
                            <div style="color: var(--text-tertiary); font-size: 0.8rem; text-transform: uppercase;">${product.category}</div>
                        </div>
                    </div>
                    <div style="display: flex; gap: 1rem;">
                        <button class="btn btn-primary" onclick="addToCart(${product.id}, event); this.closest('[style*=\"fixed\"]').remove()" style="flex: 1;">Add to Cart</button>
                        <button class="btn btn-secondary" onclick="this.closest('[style*=\"fixed\"]').remove()">Close</button>
                    </div>
                </div>
            `;
            
            modal.onclick = () => modal.remove();
            document.body.appendChild(modal);
        }

        // Show notification system
        function showNotification(message, type = 'info') {
            const notification = document.createElement('div');
            const bgColor = {
                success: 'linear-gradient(135deg, #10b981, #059669)',
                warning: 'linear-gradient(135deg, #f59e0b, #d97706)',
                error: 'linear-gradient(135deg, #ef4444, #dc2626)',
                info: 'var(--gradient-primary)'
            };
            
            notification.style.cssText = `
                position: fixed;
                top: 2rem;
                right: 2rem;
                background: ${bgColor[type]};
                color: var(--white);
                padding: 1rem 1.5rem;
                border-radius: 12px;
                box-shadow: var(--shadow-lg);
                z-index: 3000;
                font-weight: 500;
                animation: slideInRight 0.3s ease;
                max-width: 300px;
            `;
            notification.textContent = message;
            
            document.body.appendChild(notification);
            
            setTimeout(() => {
                notification.style.animation = 'slideOutRight 0.3s ease forwards';
                setTimeout(() => notification.remove(), 300);
            }, 4000);
        }

        // Toggle cart modal
        function toggleCart() {
            if (cart.length === 0) {
                showNotification('Your cart is empty. Start shopping!', 'info');
                return;
            }
            
            const total = cart.reduce((sum, item) => sum + item.price, 0);
            const modal = document.createElement('div');
            modal.style.cssText = `
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: rgba(0, 0, 0, 0.5);
                z-index: 2000;
                display: flex;
                align-items: center;
                justify-content: center;
                padding: 2rem;
                backdrop-filter: blur(8px);
            `;
            
            modal.innerHTML = `
                <div style="background: var(--bg-elevated); border-radius: 20px; padding: 2rem; max-width: 700px; width: 100%; max-height: 80vh; overflow-y: auto; border: 1px solid var(--border-light); box-shadow: var(--shadow-xl);" onclick="event.stopPropagation()">
                    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem; padding-bottom: 1rem; border-bottom: 1px solid var(--border-light);">
                        <h2 style="color: var(--text-primary); font-size: 1.5rem; font-weight: 700;">Shopping Cart (${cart.length} items)</h2>
                        <button onclick="this.closest('[style*=\"fixed\"]').remove()" style="background: none; border: none; font-size: 1.5rem; color: var(--text-tertiary); cursor: pointer;">×</button>
                    </div>
                    ${cart.map(item => `
                        <div style="display: flex; align-items: center; gap: 1rem; padding: 1rem; background: var(--surface); border-radius: 12px; margin-bottom: 1rem;">
                            <div style="font-size: 2.5rem; width: 60px; text-align: center;">${item.icon}</div>
                            <div style="flex: 1;">
                                <h4 style="color: var(--text-primary); font-weight: 600; margin-bottom: 0.25rem;">${item.title}</h4>
                                <p style="color: var(--text-secondary); font-size: 0.9rem; text-transform: capitalize;">${item.category} • ${item.rarity}</p>
                            </div>
                            <div style="text-align: right;">
                                <div style="color: var(--text-primary); font-weight: 700; font-size: 1.1rem;">${item.price.toLocaleString()}</div>
                                <button onclick="removeFromCart(${item.id}); this.closest('[style*=\"fixed\"]').remove(); toggleCart();" style="color: var(--text-tertiary); background: none; border: none; cursor: pointer; font-size: 0.8rem; margin-top: 0.25rem;">Remove</button>
                            </div>
                        </div>
                    `).join('')}
                    <div style="border-top: 1px solid var(--border-light); padding-top: 1.5rem; margin-top: 1.5rem;">
                        <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 2rem;">
                            <span style="font-size: 1.25rem; font-weight: 600; color: var(--text-primary);">Total Amount:</span>
                            <span style="font-size: 2rem; font-weight: 800; color: var(--text-accent);">${total.toLocaleString()}</span>
                        </div>
                        <div style="display: flex; gap: 1rem;">
                            <button class="btn btn-primary" style="flex: 1;" onclick="proceedToCheckout()">Proceed to Checkout</button>
                            <button class="btn btn-secondary" onclick="this.closest('[style*=\"fixed\"]').remove()">Continue Shopping</button>
                        </div>
                    </div>
                </div>
            `;
            
            modal.onclick = () => modal.remove();
            document.body.appendChild(modal);
        }

        // Remove from cart
        function removeFromCart(productId) {
            cart = cart.filter(item => item.id !== productId);
            updateCartCount();
            showNotification('Product removed from cart', 'info');
        }

        // Proceed to checkout
        function proceedToCheckout() {
            showNotification('Redirecting to secure checkout...', 'success');
            setTimeout(() => {
                showNotification('Checkout system will be available soon!', 'info');
            }, 2000);
        }

        // Toggle wishlist modal
        function toggleWishlist() {
            const wishlistItems = products.filter(p => wishlist.includes(p.id));
            
            if (wishlistItems.length === 0) {
                showNotification('Your wishlist is empty. Add some favorites!', 'info');
                return;
            }
            
            showNotification(`You have ${wishlistItems.length} items in your wishlist`, 'info');
        }

        // Scroll to top
        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // Open support
        function openSupport() {
            showNotification('Support chat will be available soon!', 'info');
        }

        // Initialize scroll effects
        function initializeScrollEffects() {
            const navbar = document.getElementById('navbar');
            
            window.addEventListener('scroll', () => {
                if (navbar && window.scrollY > 100) {
                    navbar.classList.add('scrolled');
                } else if (navbar) {
                    navbar.classList.remove('scrolled');
                }
            });
        }

        // Initialize animations
        function initializeAnimations() {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animationPlayState = 'running';
                    }
                });
            }, { 
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            });

            document.querySelectorAll('.fade-in, .slide-in').forEach(el => {
                observer.observe(el);
            });
        }

        // Add CSS animations for notifications
        const style = document.createElement('style');
        style.textContent = `
            @keyframes slideInRight {
                from { transform: translateX(100%); opacity: 0; }
                to { transform: translateX(0); opacity: 1; }
            }
            @keyframes slideOutRight {
                from { transform: translateX(0); opacity: 1; }
                to { transform: translateX(100%); opacity: 0; }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>