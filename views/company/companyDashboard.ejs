<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Membership Requests</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        custom: '#A82400',
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background-color: blanchedalmond;
        }
        .custom-color {
            color: #A82400;
        }
    </style>
</head>
<body class="min-h-screen">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <h1 class="text-3xl font-bold custom-color mb-8">Membership Requests</h1>
        
        <div class="bg-white shadow rounded-lg">
            <div class="divide-y divide-[#fdc97c]">
                <%for(let notification of notifications){%>
                    <div class="p-6 flex items-center justify-between bg-[#fee4bd] hover:bg-[#ffebcd] transition-colors cursor-pointer">
                        <div class="flex-1">
                            <div class="flex items-center space-x-4">
                                <div class="bg-white border-2 border-custom rounded-full">
                                    <img src="<%=notification.content.imageUrl%>" alt="" class="h-12 w-12 rounded-full">
                                    <!-- <svg class="h-6 w-6 text-custom" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                    </svg> -->
                                </div>
                                <div>
                                    <h2 class="text-lg font-semibold custom-color"><%=notification.content.name%></h2>
                                    <p class="text-sm custom-color opacity-75"><%=notification.content.role%></p>
                                </div>
                            </div>
                        </div>
                        <form action="/admindashboard/<%=notification._id%>/show" method="get" class="ml-4">
                            <button type="submit" class="inline-flex items-center px-4 py-2 border-2 border-custom rounded-md shadow-sm text-sm font-medium text-white bg-custom hover:bg-[#fee4bd] hover:text-custom transition-colors">
                                View Details
                                <svg class="ml-2 -mr-1 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                                </svg>
                            </button>
                        </form>
                    </div>
                <%}%>
            </div>
        </div>
    </div>
</body>
</html>