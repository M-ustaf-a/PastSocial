<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Improved Header Design</title>
    <style>
        :root {
            --primary-color: #A82400;
            --secondary-color: #fc9a06;
            --bg-light: #fee4bd;
            --bg-lighter: blanchedalmond;
            --text-dark: #333333;
            --shadow: 0 4px 12px rgba(0, 0, 0, 0.12);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--bg-lighter);
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            color: var(--text-dark);
            line-height: 1.6;
        }

        header {
            width: 100%;
            background-color: var(--bg-light);
            padding: 1rem 0;
            box-shadow: var(--shadow);
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 2px solid rgba(252, 154, 6, 0.2);
        }

        .icon-image {
            display: flex;
            align-items: center;
            justify-content: space-between;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
            height: 60px;
        }

        .icons {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .icons img {
            transition: var(--transition);
            border-radius: 50%;
            padding: 4px;
            border: 2px solid transparent;
        }

        .icons img:hover {
            transform: scale(1.1);
            border-color: var(--secondary-color);
            box-shadow: 0 2px 8px rgba(252, 154, 6, 0.3);
        }

        .logo {
            font-weight: bold;
            font-size: 1.5rem;
            color: var(--primary-color);
            margin-left: 0.75rem;
        }

        .btn {
            background: none;
            border: none;
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 50%;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .btn img {
            transition: var(--transition);
            border-radius: 50%;
        }

        .btn:hover img {
            transform: scale(1.1);
            box-shadow: 0 3px 10px rgba(168, 36, 0, 0.2);
        }

        .btn:active {
            transform: translateY(2px);
        }

        .dropdown-menu {
            background-color: var(--bg-light);
            border: 2px solid var(--secondary-color);
            border-radius: 12px;
            box-shadow: var(--shadow);
            min-width: 180px;
            opacity: 0;
            visibility: hidden;
            transform: translateY(10px);
            transition: opacity 0.3s ease, transform 0.3s ease, visibility 0.3s;
            position: absolute;
            right: 0;
            top: 100%;
            margin-top: 0.5rem;
        }

        .dropdown-toggle::after {
            display: none;
        }

        .dropdown-menu.show {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .dropdown-item {
            color: var(--primary-color);
            padding: 0.85rem 1rem;
            border-radius: 8px;
            transition: var(--transition);
            display: flex;
            align-items: center;
            text-decoration: none;
            font-weight: 500;
        }

        .dropdown-item:hover {
            background-color: var(--bg-lighter);
            color: var(--secondary-color);
            transform: translateX(5px);
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
        }

        .dropdown-item svg {
            margin-right: 8px;
        }

        @media (max-width: 768px) {
            header {
                background-color: var(--bg-lighter);
                padding: 0.5rem 0;
            }

            .icon-image {
                padding: 0 1rem;
                height: 50px;
            }

            .logo, .create-comm, .mobile-view {
                display: none;
            }

            .btn img {
                height: 36px;
                width: 36px;
            }

            .dropdown-menu {
                position: fixed;
                right: 1rem;
                top: 4rem;
                min-width: 160px;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="icon-image">
            <div class="icons">
                <img src="/group.png" alt="Community Group Icon" height="40" width="40">
            </div>
            <div class="dropdown">
                <button class="btn dropdown-toggle" type="button" aria-expanded="false">
                    <img src="/exit.png" alt="Menu" height="30px" width="30px">
                </button>
                <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="/community/<%=communityId%>/main">
                        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                            <polyline points="16 17 21 12 16 7"></polyline>
                            <line x1="21" y1="12" x2="9" y2="12"></line>
                        </svg>
                        Leave
                    </a></li>
                </ul>
            </div>
        </div>
    </header>
    <!-- Including a simple JS to make the dropdown work for demonstration -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const dropdownToggle = document.querySelector('.dropdown-toggle');
            const dropdownMenu = document.querySelector('.dropdown-menu');
            
            dropdownToggle.addEventListener('click', function() {
                dropdownMenu.classList.toggle('show');
            });
            
            // Close dropdown when clicking outside
            window.addEventListener('click', function(event) {
                if (!event.target.closest('.dropdown-toggle') && !event.target.closest('.dropdown-menu')) {
                    dropdownMenu.classList.remove('show');
                }
            });
        });
    </script>
</body>
</html>